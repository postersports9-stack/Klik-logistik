# Klik Logistik SEO Implementation Plan

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax. No unit-test harness exists in this project, so verification is `next build` + manual route/JSON-LD inspection rather than TDD.

**Goal:** Add invisible technical + structured-data SEO (and one authorized email fix) to win brand terms first, transport terms second — without changing visible copy.

**Architecture:** Next.js 16 App Router metadata API (`metadata`, `metadataBase`, `title.template`, `alternates.canonical`), file conventions (`robots.ts`, `sitemap.ts`), and a reusable server `<JsonLd>` component injecting Organization / LocalBusiness / BreadcrumbList / Service schema.

**Tech Stack:** Next.js 16, TypeScript, React 19.

**Spec:** `docs/superpowers/specs/2026-06-02-klik-seo-design.md`

**Site constants:** domain `https://klikgroup.mk`; NAP from `lib/constants/brand.ts`; address 1 бр.38, Кучевиште, Чучер-Сандево, Скопје; hours Mo-Su 09:00-17:00.

---

### Task 1: Email fix + site URL constant

**Files:**
- Modify: `lib/constants/brand.ts`

- [ ] **Step 1: Fix email and add canonical site URL**

In `lib/constants/brand.ts`, change `email: "info@kliklogistik.mk"` to `email: "info@klikgroup.mk"`, and add `url: "https://klikgroup.mk"` to the BRAND object.

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: compiles, no type errors related to BRAND.

- [ ] **Step 3: Commit**

```bash
git add lib/constants/brand.ts
git commit -m "fix: correct contact email to info@klikgroup.mk and add site url"
```

---

### Task 2: Favicon

**Files:**
- Create: `public/favicon.png` (copied from Downloads)
- Modify: `app/layout.tsx`

- [ ] **Step 1: Copy favicon into public**

Copy `C:\Users\user2\Downloads\minimalistic_Klik_Logisitka_result_3-removebg-preview.png` to `public/favicon.png`.

- [ ] **Step 2: Wire icons in layout metadata**

In `app/layout.tsx` metadata `icons`, set `icon: [{ url: "/favicon.png", type: "image/png" }]`, keep `apple: "/apple-icon.png"`.

- [ ] **Step 3: Verify**

Run: `npm run build` — passes. Confirm `public/favicon.png` exists.

- [ ] **Step 4: Commit**

```bash
git add public/favicon.png app/layout.tsx
git commit -m "feat: set brand favicon"
```

---

### Task 3: Root metadata foundation (metadataBase, template, OG/Twitter, home metadata)

**Files:**
- Modify: `app/layout.tsx`

- [ ] **Step 1: Expand root metadata**

Replace the `metadata` export in `app/layout.tsx` with:

```tsx
export const metadata: Metadata = {
  metadataBase: new URL("https://klikgroup.mk"),
  title: {
    default: "Клик Логистик (Klik Group) — Транспорт и превоз на роба Скопје",
    template: "%s | Клик Логистик",
  },
  description:
    "Клик Логистик (Klik Group) — фирма за транспорт и сигурен превоз на роба и палети во Скопје и низ цела Македонија.",
  applicationName: "Клик Логистик",
  generator: "v0.app",
  alternates: { canonical: "/" },
  openGraph: {
    type: "website",
    locale: "mk_MK",
    siteName: "Клик Логистик",
    url: "https://klikgroup.mk",
    title: "Клик Логистик (Klik Group) — Транспорт и превоз на роба Скопје",
    description:
      "Фирма за транспорт и превоз на роба и палети во Скопје и низ цела Македонија.",
    images: [{ url: "/images/hero-cover.webp", width: 1200, height: 630, alt: "Клик Логистик — превоз на роба" }],
  },
  twitter: {
    card: "summary_large_image",
    title: "Клик Логистик (Klik Group) — Транспорт Скопје",
    description: "Фирма за транспорт и превоз на роба и палети во Скопје и низ Македонија.",
    images: ["/images/hero-cover.webp"],
  },
  icons: {
    icon: [{ url: "/favicon.png", type: "image/png" }],
    apple: "/apple-icon.png",
  },
  other: { google: "notranslate" },
}
```

- [ ] **Step 2: Verify**

Run: `npm run build` — passes.

- [ ] **Step 3: Commit**

```bash
git add app/layout.tsx
git commit -m "feat: add metadataBase, title template, OpenGraph/Twitter, home metadata"
```

---

### Task 4: robots.ts and sitemap.ts

**Files:**
- Create: `app/robots.ts`
- Create: `app/sitemap.ts`

- [ ] **Step 1: Create `app/robots.ts`**

```tsx
import type { MetadataRoute } from "next"

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: "*", allow: "/" },
    sitemap: "https://klikgroup.mk/sitemap.xml",
    host: "https://klikgroup.mk",
  }
}
```

- [ ] **Step 2: Create `app/sitemap.ts`**

```tsx
import type { MetadataRoute } from "next"

export default function sitemap(): MetadataRoute.Sitemap {
  const base = "https://klikgroup.mk"
  const now = new Date()
  return [
    { url: `${base}/`, lastModified: now, changeFrequency: "weekly", priority: 1 },
    { url: `${base}/klik-trejd`, lastModified: now, changeFrequency: "monthly", priority: 0.8 },
    { url: `${base}/za-nas`, lastModified: now, changeFrequency: "monthly", priority: 0.7 },
    { url: `${base}/nasata-rabota`, lastModified: now, changeFrequency: "monthly", priority: 0.7 },
  ]
}
```

- [ ] **Step 3: Verify**

Run: `npm run build`. Then `npm start` (or check build output) and confirm `/robots.txt` and `/sitemap.xml` are generated.

- [ ] **Step 4: Commit**

```bash
git add app/robots.ts app/sitemap.ts
git commit -m "feat: add robots.txt and sitemap.xml"
```

---

### Task 5: JsonLd component + Organization (sitewide) + LocalBusiness (home)

**Files:**
- Create: `components/seo/json-ld.tsx`
- Modify: `app/layout.tsx`
- Modify: `app/page.tsx`

- [ ] **Step 1: Create `components/seo/json-ld.tsx`**

```tsx
export function JsonLd({ data }: { data: Record<string, unknown> }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  )
}

export const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "Klik Logistik",
  alternateName: ["Klik Group", "Клик Логистик", "Клик Груп", "Klik Logistik DOOEL Skopje"],
  url: "https://klikgroup.mk",
  logo: "https://klikgroup.mk/favicon.png",
  email: "info@klikgroup.mk",
  telephone: "+389 70 233 465",
  sameAs: [],
}

export const localBusinessSchema = {
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  name: "Клик Логистик",
  alternateName: ["Klik Logistik", "Klik Group"],
  url: "https://klikgroup.mk",
  image: "https://klikgroup.mk/images/hero-cover.webp",
  email: "info@klikgroup.mk",
  telephone: "+389 70 233 465",
  priceRange: "$$",
  address: {
    "@type": "PostalAddress",
    streetAddress: "1 бр.38, Кучевиште",
    addressLocality: "Чучер-Сандево",
    addressRegion: "Скопје",
    addressCountry: "MK",
  },
  areaServed: "Северна Македонија",
  openingHours: "Mo-Su 09:00-17:00",
}
```

- [ ] **Step 2: Inject Organization sitewide in layout**

In `app/layout.tsx`, import `JsonLd, organizationSchema` and render `<JsonLd data={organizationSchema} />` inside `<body>` (before `{children}`).

- [ ] **Step 3: Inject LocalBusiness on home**

In `app/page.tsx`, import `JsonLd, localBusinessSchema` and render `<JsonLd data={localBusinessSchema} />` at the top of `<main>`.

- [ ] **Step 4: Verify**

Run: `npm run build` — passes. Manually copy each schema object into Google Rich Results Test later.

- [ ] **Step 5: Commit**

```bash
git add components/seo/json-ld.tsx app/layout.tsx app/page.tsx
git commit -m "feat: add Organization and LocalBusiness JSON-LD"
```

---

### Task 6: Per-page metadata + breadcrumbs + Service schema

**Files:**
- Modify: `app/klik-trejd/page.tsx`
- Modify: `app/za-nas/page.tsx`
- Modify: `app/nasata-rabota/page.tsx`

- [ ] **Step 1: klik-trejd metadata + Service + breadcrumb**

Replace metadata in `app/klik-trejd/page.tsx` with:

```tsx
export const metadata = {
  title: "Клик Трејд (Klik Trejd) — Откуп и продажба на палети",
  description: "Клик Трејд — откуп и продажба на дрвени (120x80) и индустриски палети низ цела Македонија. Транспорт на палети.",
  alternates: { canonical: "/klik-trejd" },
}
```

Add import `JsonLd` and render at top of `<main>`:

```tsx
<JsonLd data={{
  "@context": "https://schema.org",
  "@type": "Service",
  serviceType: "Откуп и продажба на палети",
  provider: { "@type": "Organization", name: "Klik Logistik", url: "https://klikgroup.mk" },
  areaServed: "Северна Македонија",
  name: "Клик Трејд — палети",
}} />
<JsonLd data={{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: "Дома", item: "https://klikgroup.mk/" },
    { "@type": "ListItem", position: 2, name: "Клик Трејд", item: "https://klikgroup.mk/klik-trejd" },
  ],
}} />
```

- [ ] **Step 2: za-nas metadata + breadcrumb**

Replace metadata in `app/za-nas/page.tsx` with:

```tsx
export const metadata = {
  title: "За нас — Клик Логистик, фирма за транспорт во Скопје",
  description: "Дознајте за Клик Логистик (Klik Group) — фирма за транспорт и логистика за сигурен превоз на роба во Скопје и низ Македонија.",
  alternates: { canonical: "/za-nas" },
}
```

Add import `JsonLd` and render at top of `<main>`:

```tsx
<JsonLd data={{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: "Дома", item: "https://klikgroup.mk/" },
    { "@type": "ListItem", position: 2, name: "За нас", item: "https://klikgroup.mk/za-nas" },
  ],
}} />
```

- [ ] **Step 3: nasata-rabota — ADD metadata + breadcrumb**

`app/nasata-rabota/page.tsx` has no metadata. Add at top (after imports):

```tsx
export const metadata = {
  title: "Нашата работа — Клик Логистик, транспорт МК",
  description: "Портфолио и историја на Клик Логистик — транспорт и превоз на роба и палети низ цела Македонија.",
  alternates: { canonical: "/nasata-rabota" },
}
```

Add import `JsonLd` and render at top of returned root element:

```tsx
<JsonLd data={{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: "Дома", item: "https://klikgroup.mk/" },
    { "@type": "ListItem", position: 2, name: "Нашата работа", item: "https://klikgroup.mk/nasata-rabota" },
  ],
}} />
```

- [ ] **Step 4: Verify**

Run: `npm run build` — passes, all 4 routes build.

- [ ] **Step 5: Commit**

```bash
git add app/klik-trejd/page.tsx app/za-nas/page.tsx app/nasata-rabota/page.tsx
git commit -m "feat: add per-page metadata, breadcrumbs, and Service schema"
```

---

### Task 7: Final build verification

- [ ] **Step 1: Full build**

Run: `npm run build`
Expected: all routes compile; `/robots.txt`, `/sitemap.xml` listed in output.

- [ ] **Step 2: Report**

Summarize what shipped. Note manual follow-ups for the client: validate JSON-LD in Google Rich Results Test, submit `sitemap.xml` in Search Console, then review visible-copy proposals (Task 8, separate).

---

## Deferred (separate approval gate — NOT in this build)

**Task 8 — Visible copy proposals + alt-text pass.** After foundation merges, read current hero/heading copy and image alts, draft a short diff weaving in secondary keywords (транспорт Скопје, фирма за транспорт) and improving alts. Present to client for per-item approval. No edits before approval.
